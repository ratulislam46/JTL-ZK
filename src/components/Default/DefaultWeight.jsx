import React from 'react';

const DefaultWeight = ({ children }) => {
    return (
        <div className='container mx-2 lg:mx-auto my-12 lg:my-24'>
            {children}
        </div>
    );
};

export default DefaultWeight;